[project]
name = "g-kv-cache-exp"
version = "0.1.0"
description = "Live KV Cache & Prefix Caching Demonstration"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "vllm==0.9.2",
    "rich>=13.9.4",
    "tiktoken>=0.12.0",
]

[tool.uv]
no-build-isolation-package = ["vllm"]
